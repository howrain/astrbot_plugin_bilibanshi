{
    "auto_start": {
        "description": "开机自启动",
        "type": "bool",
        "default": true,
        "hint": "启动 AstrBot 时自动开始定时搬石"
    },
    "scan_interval": {
        "description": "扫描间隔（秒）",
        "type": "int",
        "default": 60,
        "hint": "每次定时搬石之间的等待时间，单位为秒，最小10秒"
    },
    "max_duration": {
        "description": "最大视频时长（秒）",
        "type": "int",
        "default": 600,
        "hint": "超过此时长的视频会被跳过，默认600秒（10分钟）"
    },
    "max_pages": {
        "description": "搜索页数",
        "type": "int",
        "default": 5,
        "hint": "关键词模式下最多搜索几页；UP主UID模式下每个投稿列表最多读取几页。页数越多可选视频越多，但请求也越多"
    },
    "bilibili_cookie": {
        "description": "B站 Cookie（可选）",
        "type": "string",
        "default": "",
        "secret": true,
        "hint": "可粘贴浏览器请求中的 Cookie 内容，支持带或不带 Cookie: 前缀。Cookie 是登录凭证，请勿分享；配置为空时继续使用匿名请求"
    },
    "video_quality": {
        "description": "视频画质",
        "type": "int",
        "default": 64,
        "hint": "B站视频画质等级：16=360P, 32=480P, 64=720P, 80=1080P。画质越高文件越大，QQ等平台发送失败率越高，建议默认720P"
    },
    "transcode": {
        "description": "强制转码为 H.264 main profile",
        "type": "bool",
        "default": false,
        "hint": "开启后所有视频都会用 ffmpeg 转码为 H.264 main profile（兼容性最好，但更耗CPU）。关闭时仅非 H.264 源转码"
    },
    "delete_after_send": {
        "description": "发送后删除视频文件",
        "type": "bool",
        "default": true,
        "hint": "发送视频后是否自动删除本地文件，建议开启以节省磁盘空间"
    },
    "search_keywords": {
        "description": "搜索关键词列表",
        "type": "list",
        "default": [
            "咕咕嘎嘎",
            "凑企鹅",
            "没人看懂",
            "旮旯给木",
            "搬石",
            "高松灯"
        ],
        "hint": "B站搜索使用的关键词，每次随机选一个关键词搜索视频"
    },
    "selection_mode": {
        "description": "视频来源筛选方式",
        "type": "string",
        "options": ["关键词", "UP主UID", "两者随机"],
        "default": "关键词",
        "hint": "关键词：从搜索关键词中搬运；UP主UID：从指定UP主投稿中搬运；两者随机：每轮先以相同概率选择关键词或UP主来源，再从该来源配置中随机选择"
    },
    "up_mids": {
        "description": "UP主UID列表",
        "type": "list",
        "default": [],
        "hint": "每项填写 UID 或 UID:备注，例如 123456 或 123456:喜欢的UP。备注只用于配置时识别，不参与请求；两者随机模式需同时配置关键词和UID"
    },
    "blacklist_groups": {
        "description": "黑名单群号列表",
        "type": "list",
        "default": [],
        "hint": "这些群不会收到定时搬石的视频推送"
    },
    "use_whitelist_mode": {
        "description": "启用白名单模式",
        "type": "bool",
        "default": true,
        "hint": "开启后只有白名单中的群会收到视频推送；关闭则为黑名单模式"
    },
    "whitelist_groups": {
        "description": "白名单群号列表",
        "type": "list",
        "default": [],
        "hint": "白名单模式下，只有这些群会收到定时搬石的视频推送"
    },
    "quiet_hours_start": {
        "description": "免打扰开始时间",
        "type": "string",
        "default": "",
        "hint": "格式 HH:MM，如 23:00。留空表示不启用免打扰。在此时段内定时任务不会推送视频"
    },
    "quiet_hours_end": {
        "description": "免打扰结束时间",
        "type": "string",
        "default": "",
        "hint": "格式 HH:MM，如 08:00。支持跨午夜，如开始23:00结束08:00表示晚11点到早8点不推送"
    }
}
